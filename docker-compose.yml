version: '3'
services:
  client:
    container_name: client
    image: client:latest
    entrypoint: python3 main.py
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - config
    volumes:
      - ./src:/src
      - ./results:/src/results
    network_mode: host
